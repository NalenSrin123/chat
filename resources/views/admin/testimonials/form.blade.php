@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.testimonials.index') }}">← Testimonials</a><h1 class="mt-4 text-4xl font-bold">{{ $testimonial->exists ? 'Edit' : 'Add' }} testimonial</h1><form method="POST" action="{{ $action }}" enctype="multipart/form-data" class="admin-card mt-8 grid gap-5 p-6 md:grid-cols-2">@csrf @if($testimonial->exists) @method('PUT') @endif<label class="text-sm">Name<input required name="name" value="{{ old('name',$testimonial->name) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="text-sm">Position<input required name="position" value="{{ old('position',$testimonial->position) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="text-sm">Company<input name="company" value="{{ old('company',$testimonial->company) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="text-sm">Sort order<input name="sort_order" type="number" value="{{ old('sort_order',$testimonial->sort_order ?? 0) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="text-sm md:col-span-2">Message<textarea required name="message" rows="6" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">{{ old('message',$testimonial->message) }}</textarea></label><label class="text-sm">Avatar<input type="file" name="avatar" accept="image/*" class="mt-2 block">@if($testimonial->avatar)<img src="{{ asset('storage/'.$testimonial->avatar) }}" class="mt-3 h-16 w-16 rounded-full object-cover" alt="Avatar">@endif</label><label class="flex items-center gap-2 pt-7 text-sm"><input type="checkbox" name="is_approved" value="1" @checked(old('is_approved',$testimonial->is_approved))> Approved for public display</label><button class="w-fit rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Save testimonial</button></form></main>@endsection
