@extends('layouts.admin') @section('content')<main class="admin-profile max-w-3xl p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.skills.index') }}">← Skills</a><h1 class="mt-4 text-4xl font-bold">{{ $skill->exists ? 'Edit' : 'Add' }} skill</h1><form method="POST" action="{{ $action }}" class="admin-card mt-8 space-y-5 p-6">@csrf @if($skill->exists) @method('PUT') @endif<label class="block text-sm">Category<select required name="skill_category_id" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('skill_category_id',$skill->skill_category_id) == $category->id)>{{ $category->name }}</option>@endforeach</select>@error('skill_category_id')<span class="text-xs text-red-600">{{ $message }}</span>@enderror</label><label class="block text-sm">Name<input required name="name" value="{{ old('name',$skill->name) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="block text-sm">Icon name (optional)<input name="icon" value="{{ old('icon',$skill->icon) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="block text-sm">Sort order<input name="sort_order" type="number" value="{{ old('sort_order',$skill->sort_order ?? 0) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><button class="rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Save skill</button></form></main>@endsection
