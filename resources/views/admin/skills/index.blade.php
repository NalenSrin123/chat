@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="font-mono text-sm text-cyan-600">ADMIN / SKILLS</p><h1 class="mt-3 text-4xl font-bold">Skills</h1></div><a href="{{ route('admin.skills.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 text-sm font-semibold text-white">+ Add skill</a></div><form class="mt-6"><select name="category" onchange="this.form.submit()" class="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category') == $category->id)>{{ $category->name }}</option>@endforeach</select></form>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="admin-card mt-6 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Order</th><th class="px-6 py-4">Skill</th><th class="px-6 py-4">Category</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($skills as $skill)<tr><td class="px-6 py-4">{{ $skill->sort_order }}</td><td class="px-6 py-4 font-semibold">{{ $skill->icon }} {{ $skill->name }}</td><td class="px-6 py-4">{{ $skill->category->name }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.skills.edit',$skill) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.skills.destroy',$skill) }}" onsubmit="return confirm('Delete this skill?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="4" class="px-6 py-10 text-center">No skills yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $skills->links() }}</div></main>@endsection
