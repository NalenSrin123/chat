@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div class="flex items-end justify-between"><div><p class="font-mono text-sm text-cyan-600">ADMIN / BLOG</p><h1 class="mt-3 text-4xl font-bold">Blog posts</h1></div><a href="{{ route('admin.blog.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 font-semibold text-white">+ Add post</a></div><form class="mt-6 flex flex-wrap gap-2"><input name="q" value="{{ request('q') }}" placeholder="Search posts" class="rounded-lg border border-slate-200 px-4 py-2"><select name="status" class="rounded-lg border border-slate-200 px-4 py-2"><option value="">All statuses</option><option value="draft" @selected(request('status')==='draft')>Draft</option><option value="published" @selected(request('status')==='published')>Published</option></select><select name="category" class="rounded-lg border border-slate-200 px-4 py-2"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="admin-card mt-6 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Title</th><th class="px-6 py-4">Categories</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Published</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($blogs as $blog)<tr><td class="px-6 py-4 font-semibold">{{ $blog->title }}</td><td class="px-6 py-4">{{ $blog->categories->pluck('name')->join(', ') ?: '—' }}</td><td class="px-6 py-4 capitalize">{{ $blog->status }}</td><td class="px-6 py-4">{{ $blog->published_at?->format('M d, Y') ?? '—' }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.blog.edit',$blog) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.blog.destroy',$blog) }}" onsubmit="return confirm('Delete this blog post?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-6 py-10 text-center">No blog posts found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $blogs->links() }}</div></main>@endsection
