@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.blog.index') }}">← Blog posts</a><h1 class="mt-4 text-4xl font-bold">{{ $blog->exists ? 'Edit' : 'Add' }} blog post</h1><form method="POST" action="{{ $action }}" enctype="multipart/form-data" class="mt-8 space-y-6">@csrf @if($blog->exists) @method('PUT') @endif<section class="admin-card grid gap-5 p-6 md:grid-cols-2"><h2 class="md:col-span-2 text-xl font-semibold">Post details</h2><label class="text-sm">Title<input required name="title" value="{{ old('title',$blog->title) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="text-sm">Slug<input name="slug" value="{{ old('slug',$blog->slug) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="text-sm md:col-span-2">Excerpt<textarea name="excerpt" rows="3" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">{{ old('excerpt',$blog->excerpt) }}</textarea></label><label class="text-sm md:col-span-2">Content<textarea required name="content" rows="14" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">{{ old('content',$blog->content) }}</textarea></label><label class="text-sm">Status<select name="status" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="draft" @selected(old('status',$blog->status ?: 'draft')==='draft')>Draft</option><option value="published" @selected(old('status',$blog->status)==='published')>Published</option></select></label><label class="text-sm">Published at<input type="datetime-local" name="published_at" value="{{ old('published_at',$blog->published_at?->format('Y-m-d\\TH:i')) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label></section><section class="admin-card p-6"><h2 class="text-xl font-semibold">Categories</h2><div class="mt-5 grid gap-3 sm:grid-cols-3">@foreach($categories as $category)<label class="flex items-center gap-2 text-sm"><input type="checkbox" name="category_ids[]" value="{{ $category->id }}" @checked(in_array($category->id,old('category_ids',$blog->categories->pluck('id')->all())))>{{ $category->name }}</label>@endforeach</div></section><section class="admin-card p-6"><h2 class="text-xl font-semibold">Cover image</h2><input type="file" name="cover_image" accept="image/*" class="mt-4 block">@if($blog->cover_image)<img src="{{ asset('storage/'.$blog->cover_image) }}" class="mt-4 h-40 rounded-xl object-cover" alt="Cover">@endif</section><button class="rounded-lg bg-sky-500 px-6 py-3 font-semibold text-white">Save post</button></form></main>@endsection
