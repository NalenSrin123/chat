@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><p class="font-mono text-sm text-cyan-600">ADMIN / RESUME</p><h1 class="mt-3 text-4xl font-bold">Resume / CV</h1><section class="admin-card mt-8 max-w-2xl p-6"><p class="text-sm">{{ $profile->cv_path ? 'A CV is currently uploaded.' : 'No CV uploaded yet.' }}</p><form method="POST" action="{{ route('admin.resume.update') }}" enctype="multipart/form-data" class="mt-6 flex flex-wrap items-end gap-5">@csrf<label class="text-sm">PDF file<input required type="file" name="cv" accept="application/pdf" class="mt-2 block"></label><button class="rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">{{ $profile->cv_path ? 'Replace CV' : 'Upload CV' }}</button></form>@if($profile->cv_path)<form method="POST" action="{{ route('admin.resume.destroy') }}" class="mt-5">@csrf @method('DELETE')<button class="text-sm text-red-600">Remove current CV</button></form>@endif</section></main>@endsection
