@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div class="flex items-end justify-between"><div><p class="font-mono text-sm text-cyan-600">ADMIN / EDUCATION</p><h1 class="mt-3 text-4xl font-bold">Education</h1></div><a href="{{ route('admin.education.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 font-semibold text-white">+ Add education</a></div><div class="admin-card mt-8 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Order</th><th class="px-6 py-4">School</th><th class="px-6 py-4">Degree / Major</th><th class="px-6 py-4">Period</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($educations as $item)<tr><td class="px-6 py-4">{{ $item->sort_order }}</td><td class="px-6 py-4 font-semibold">{{ $item->school }}</td><td class="px-6 py-4">{{ $item->degree }} · {{ $item->major }}</td><td class="px-6 py-4">{{ $item->start_date?->format('Y') }} — {{ $item->end_date?->format('Y') ?? 'Present' }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.education.edit',$item) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.education.destroy',$item) }}" onsubmit="return confirm('Delete this education record?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-6 py-10 text-center">No education records yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $educations->links() }}</div></main>@endsection
