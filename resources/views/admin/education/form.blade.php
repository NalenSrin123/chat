@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.education.index') }}">← Education</a><h1 class="mt-4 text-4xl font-bold">{{ $education->exists ? 'Edit' : 'Add' }} education</h1><form method="POST" action="{{ $action }}" class="admin-card mt-8 grid gap-5 p-6 md:grid-cols-2">@csrf @if($education->exists) @method('PUT') @endif @foreach([['school','School / university'],['degree','Degree'],['major','Major'],['start_date','Start date'],['end_date','End date'],['sort_order','Sort order']] as [$field,$label])<label class="text-sm">{{ $label }}<input name="{{ $field }}" type="{{ str_contains($field,'date')?'date':($field==='sort_order'?'number':'text') }}" value="{{ old($field,$education->{$field}) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">@error($field)<span class="text-xs text-red-600">{{ $message }}</span>@enderror</label>@endforeach<label class="text-sm md:col-span-2">Description<textarea name="description" rows="6" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">{{ old('description',$education->description) }}</textarea></label><button class="w-fit rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Save education</button></form></main>@endsection
