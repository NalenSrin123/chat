@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div class="flex items-end justify-between"><div><p class="font-mono text-sm text-cyan-600">ADMIN / SOCIAL LINKS</p><h1 class="mt-3 text-4xl font-bold">Social links</h1></div><a href="{{ route('admin.social-links.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 text-sm font-semibold text-white">+ Add link</a></div>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="admin-card mt-8 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Order</th><th class="px-6 py-4">Name</th><th class="px-6 py-4">URL</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($links as $link)<tr><td class="px-6 py-4">{{ $link->sort_order }}</td><td class="px-6 py-4 font-semibold">{{ $link->name }}</td><td class="max-w-xs truncate px-6 py-4 text-slate-500">{{ $link->url }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.social-links.edit',$link) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.social-links.destroy',$link) }}" onsubmit="return confirm('Delete this social link?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="4" class="px-6 py-10 text-center">No social links yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $links->links() }}</div></main>@endsection
