@extends('layouts.admin') @section('content')<main class="admin-profile max-w-3xl p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.social-links.index') }}">← Social links</a><h1 class="mt-4 text-4xl font-bold">{{ $link->exists ? 'Edit' : 'Add' }} social link</h1><form method="POST" action="{{ $action }}" class="admin-card mt-8 space-y-5 p-6">@csrf @if($link->exists) @method('PUT') @endif @foreach([['name','Name','text'],['url','URL','url'],['icon','Icon name (optional)','text'],['sort_order','Sort order','number']] as [$field,$label,$type])<label class="block text-sm">{{ $label }}<input name="{{ $field }}" type="{{ $type }}" value="{{ old($field,$link->{$field}) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">@error($field)<span class="text-xs text-red-600">{{ $message }}</span>@enderror</label>@endforeach<button class="rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Save link</button></form></main>@endsection
