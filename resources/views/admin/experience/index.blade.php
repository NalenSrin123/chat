@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div class="flex items-end justify-between"><div><p class="font-mono text-sm text-cyan-600">ADMIN / EXPERIENCE</p><h1 class="mt-3 text-4xl font-bold">Experience</h1></div><a href="{{ route('admin.experience.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 font-semibold text-white">+ Add experience</a></div><div class="admin-card mt-8 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Order</th><th class="px-6 py-4">Position</th><th class="px-6 py-4">Company</th><th class="px-6 py-4">Period</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($experiences as $item)<tr><td class="px-6 py-4">{{ $item->sort_order }}</td><td class="px-6 py-4 font-semibold">{{ $item->position }}</td><td class="px-6 py-4">{{ $item->company }}</td><td class="px-6 py-4">{{ $item->start_date?->format('M Y') }} — {{ $item->is_current ? 'Present' : $item->end_date?->format('M Y') }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.experience.edit',$item) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.experience.destroy',$item) }}" onsubmit="return confirm('Delete this experience?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-6 py-10 text-center">No experience records yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $experiences->links() }}</div></main>@endsection
