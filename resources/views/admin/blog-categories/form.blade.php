@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.blog-categories.index') }}">← Blog categories</a><h1 class="mt-4 text-4xl font-bold">{{ $category->exists ? 'Edit' : 'Add' }} category</h1><form method="POST" action="{{ $action }}" class="admin-card mt-8 space-y-5 p-6">@csrf @if($category->exists) @method('PUT') @endif<label class="block text-sm">Name<input required name="name" value="{{ old('name',$category->name) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="block text-sm">Slug (optional)<input name="slug" value="{{ old('slug',$category->slug) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><label class="block text-sm">Description<textarea name="description" rows="5" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">{{ old('description',$category->description) }}</textarea></label><button class="rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Save category</button></form></main>@endsection
