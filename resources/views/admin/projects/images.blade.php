@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.projects.index') }}">← Projects</a><h1 class="mt-4 text-4xl font-bold">{{ $project->title }} screenshots</h1>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<form method="POST" action="{{ route('admin.projects.images.store',$project) }}" enctype="multipart/form-data" class="admin-card mt-8 flex flex-wrap items-end gap-5 p-6">@csrf<label class="text-sm">Upload multiple screenshots<input required type="file" name="images[]" multiple accept="image/*" class="mt-2 block"></label><button class="rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Upload</button></form><div class="mt-8 grid gap-6 md:grid-cols-2 xl:grid-cols-3">@forelse($project->projectImages->sortBy('sort_order') as $image)<div class="admin-card overflow-hidden"><img src="{{ asset('storage/'.$image->image_path) }}" class="h-48 w-full object-cover" alt="{{ $image->caption }}"><form method="POST" action="{{ route('admin.projects.images.update',[$project,$image]) }}" class="space-y-3 p-4">@csrf @method('PUT')<input name="caption" value="{{ $image->caption }}" placeholder="Caption" class="w-full rounded-lg border border-slate-200 px-3 py-2"><input type="number" name="sort_order" value="{{ $image->sort_order }}" class="w-full rounded-lg border border-slate-200 px-3 py-2"><button class="text-sm font-semibold text-sky-600">Save</button></form><form method="POST" action="{{ route('admin.projects.images.destroy',[$project,$image]) }}" class="px-4 pb-4">@csrf @method('DELETE')<button class="text-sm text-red-600">Delete screenshot</button></form></div>@empty<p class="text-slate-600">No screenshots uploaded yet.</p>@endforelse</div></main>@endsection
