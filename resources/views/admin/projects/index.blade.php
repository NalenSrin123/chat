@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div class="flex items-end justify-between"><div><p class="font-mono text-sm text-cyan-600">ADMIN / PROJECTS</p><h1 class="mt-3 text-4xl font-bold">Projects</h1></div><a href="{{ route('admin.projects.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 text-sm font-semibold text-white">+ Add project</a></div><form class="mt-6 flex flex-wrap gap-2"><input name="q" value="{{ request('q') }}" placeholder="Search projects" class="rounded-lg border border-slate-200 px-4 py-2"><select name="status" class="rounded-lg border border-slate-200 px-4 py-2"><option value="">All statuses</option>@foreach(['draft','in_progress','completed','archived'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst(str_replace('_',' ',$status)) }}</option>@endforeach</select><select name="featured" class="rounded-lg border border-slate-200 px-4 py-2"><option value="">All projects</option><option value="1" @selected(request('featured')==='1')>Featured</option><option value="0" @selected(request('featured')==='0')>Not featured</option></select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="admin-card mt-6 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Project</th><th class="px-6 py-4">Technologies</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Featured</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($projects as $project)<tr><td class="px-6 py-4"><p class="font-semibold">{{ $project->title }}</p><p class="text-xs text-slate-500">/{{ $project->slug }}</p></td><td class="px-6 py-4">{{ $project->technologies->pluck('name')->join(', ') ?: '—' }}</td><td class="px-6 py-4 capitalize">{{ str_replace('_',' ',$project->status) }}</td><td class="px-6 py-4">{{ $project->featured ? 'Yes' : 'No' }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.projects.edit',$project) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.projects.destroy',$project) }}" onsubmit="return confirm('Delete this project and its images?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-6 py-10 text-center">No projects found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $projects->links() }}</div></main>@endsection
