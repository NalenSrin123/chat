@extends('layouts.admin')
@section('content')
<main class="admin-profile p-5 lg:p-8"><div class="flex items-end justify-between"><div><p class="font-mono text-sm text-cyan-600">ADMIN / CERTIFICATES</p><h1 class="mt-3 text-4xl font-bold">Certificates</h1></div><a href="{{ route('admin.certificates.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 font-semibold text-white">+ Add certificate</a></div>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="admin-card mt-8 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Image</th><th class="px-6 py-4">Certificate</th><th class="px-6 py-4">Issuer</th><th class="px-6 py-4">Issue date</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($certificates as $item)<tr><td class="px-6 py-4">@if($item->image_path)<a href="{{ asset('storage/'.$item->image_path) }}" target="_blank"><img src="{{ asset('storage/'.$item->image_path) }}?v={{ $item->updated_at?->timestamp }}" class="h-14 w-20 rounded-lg object-cover" alt="{{ $item->title }}"></a>@else<span class="text-slate-400">No image</span>@endif</td><td class="px-6 py-4 font-semibold">{{ $item->title }}</td><td class="px-6 py-4">{{ $item->issuer }}</td><td class="px-6 py-4">{{ $item->issue_date?->format('M Y') }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.certificates.edit',$item) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.certificates.destroy',$item) }}" onsubmit="return confirm('Delete this certificate?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-6 py-10 text-center">No certificates yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $certificates->links() }}</div></main>
@endsection
