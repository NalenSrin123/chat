@extends('layouts.admin') @section('content')<main class="admin-profile max-w-3xl p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.skill-categories.index') }}">← Skill categories</a><h1 class="mt-4 text-4xl font-bold">{{ $category->exists ? 'Edit' : 'Add' }} category</h1><form method="POST" action="{{ $action }}" class="admin-card mt-8 space-y-5 p-6">@csrf @if($category->exists) @method('PUT') @endif<label class="block text-sm">Name<input name="name" value="{{ old('name',$category->name) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">@error('name')<span class="text-xs text-red-600">{{ $message }}</span>@enderror</label><label class="block text-sm">Description<textarea name="description" rows="4" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3">{{ old('description',$category->description) }}</textarea></label><label class="block text-sm">Sort order<input name="sort_order" type="number" value="{{ old('sort_order',$category->sort_order ?? 0) }}" class="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></label><button class="rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Save category</button></form></main>@endsection
