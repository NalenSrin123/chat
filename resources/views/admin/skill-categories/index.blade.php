@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div class="flex items-end justify-between"><div><p class="font-mono text-sm text-cyan-600">ADMIN / SKILL CATEGORIES</p><h1 class="mt-3 text-4xl font-bold">Skill categories</h1></div><a href="{{ route('admin.skill-categories.create') }}" class="rounded-lg bg-sky-500 px-4 py-2 text-sm font-semibold text-white">+ Add category</a></div>@if(session('error'))<div class="mt-5 rounded-lg bg-red-50 p-4 text-red-700">{{ session('error') }}</div>@endif @if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="admin-card mt-8 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Order</th><th class="px-6 py-4">Name</th><th class="px-6 py-4">Skills</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($categories as $category)<tr><td class="px-6 py-4">{{ $category->sort_order }}</td><td class="px-6 py-4 font-semibold">{{ $category->name }}</td><td class="px-6 py-4">{{ $category->skills_count }}</td><td class="px-6 py-4 text-right"><a class="mr-3 text-sky-600" href="{{ route('admin.skill-categories.edit',$category) }}">Edit</a><form class="inline" method="POST" action="{{ route('admin.skill-categories.destroy',$category) }}" onsubmit="return confirm('Delete this category?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="4" class="px-6 py-10 text-center">No categories yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $categories->links() }}</div></main>@endsection
