@extends('layouts.admin')
@section('content')
<main class="admin-profile p-5 lg:p-8"><a class="text-sm text-sky-600" href="{{ route('admin.messages.index') }}">← Inbox</a>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="mt-5 flex flex-wrap items-start justify-between gap-4"><div><p class="font-mono text-sm text-cyan-600">MESSAGE</p><h1 class="mt-2 text-4xl font-bold">{{ $message->subject }}</h1><p class="mt-2 text-slate-500">From {{ $message->name }} · {{ $message->email }} · {{ $message->created_at->format('M d, Y H:i') }}</p></div><span class="rounded-full bg-sky-50 px-3 py-1 text-sm font-semibold capitalize text-sky-600">{{ $message->status }}</span></div><article class="admin-card mt-8 whitespace-pre-line p-8 leading-8">{{ $message->message }}</article><section class="admin-card mt-6 p-6"><h2 class="text-xl font-semibold">Reply to {{ $message->email }}</h2><form method="POST" action="{{ route('admin.messages.reply',$message) }}" class="mt-5">@csrf<textarea required name="reply" rows="8" placeholder="Write your reply..." class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"></textarea>@error('reply')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror<button class="mt-4 rounded-lg bg-sky-500 px-5 py-3 font-semibold text-white">Send reply</button></form></section><div class="mt-6 flex flex-wrap gap-3"><form method="POST" action="{{ route('admin.messages.status',$message) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="read"><button class="rounded-lg border border-slate-200 px-4 py-2 text-sm">Mark read</button></form><form method="POST" action="{{ route('admin.messages.status',$message) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="replied"><button class="rounded-lg bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Mark replied</button></form><form method="POST" action="{{ route('admin.messages.destroy',$message) }}" onsubmit="return confirm('Delete this message?')">@csrf @method('DELETE')<button class="rounded-lg bg-red-600 px-4 py-2 text-sm font-semibold text-white">Delete</button></form></div></main>
@endsection
