@extends('layouts.admin') @section('content')<main class="admin-profile p-5 lg:p-8"><div><p class="font-mono text-sm text-cyan-600">ADMIN / INBOX</p><h1 class="mt-3 text-4xl font-bold">Contact messages</h1></div><form class="mt-6 flex flex-wrap gap-2"><input name="q" value="{{ request('q') }}" placeholder="Search sender, email, subject" class="rounded-lg border border-slate-200 px-4 py-2"><select name="status" class="rounded-lg border border-slate-200 px-4 py-2"><option value="">All statuses</option>@foreach(['new','read','replied'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><select name="sort" class="rounded-lg border border-slate-200 px-4 py-2"><option value="newest" @selected(request('sort','newest')==='newest')>Newest first</option><option value="oldest" @selected(request('sort')==='oldest')>Oldest first</option></select><button class="rounded-lg bg-slate-800 px-4 py-2 text-white">Filter</button></form>@if(session('success'))<div class="mt-5 rounded-lg bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="admin-card mt-6 overflow-hidden"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Sender</th><th class="px-6 py-4">Subject</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Received</th><th class="px-6 py-4 text-right">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($messages as $message)<tr class="{{ $message->status==='new' ? 'bg-sky-50/60 font-medium' : '' }}"><td class="px-6 py-4"><p>{{ $message->name }}</p><p class="text-xs text-slate-500">{{ $message->email }}</p></td><td class="px-6 py-4">{{ $message->subject }}</td><td class="px-6 py-4 capitalize">{{ $message->status }}</td><td class="px-6 py-4">{{ $message->created_at->format('M d, Y H:i') }}</td><td class="px-6 py-4 text-right"><a class="text-sky-600" href="{{ route('admin.messages.show',$message) }}">Open</a></td></tr>@empty<tr><td colspan="5" class="px-6 py-10 text-center">No messages found.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $messages->links() }}</div></main>@endsection
