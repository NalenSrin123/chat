@extends('layouts.portfolio')
@section('content')
<section class="mx-auto max-w-7xl px-6 py-24"><div class="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p class="font-mono text-sm text-cyan-300">03 — Selected work</p><h1 class="mt-3 text-5xl font-bold text-white">Projects</h1></div><form class="flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search projects" class="rounded-full border border-white/15 bg-white/5 px-4 py-3 text-sm outline-none"><select name="technology" class="rounded-full border border-white/15 bg-[#101d30] px-4 py-3 text-sm"><option value="">All technologies</option>@foreach($technologies as $technology)<option value="{{ $technology->name }}" @selected(request('technology') === $technology->name)>{{ $technology->name }}</option>@endforeach</select><select name="status" class="rounded-full border border-white/15 bg-[#101d30] px-4 py-3 text-sm"><option value="">All statuses</option>@foreach(['in_progress','completed','archived'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst(str_replace('_',' ',$status)) }}</option>@endforeach</select><label class="flex items-center gap-2 rounded-full border border-white/15 px-4 text-sm"><input type="checkbox" name="featured" value="1" @checked(request('featured') === '1')> Featured</label><button class="rounded-full bg-cyan-300 px-5 py-3 text-sm font-semibold text-slate-950">Filter</button></form></div><div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse($projects as $project)<a href="{{ route('projects.show',$project) }}" class="rounded-3xl border border-white/10 bg-white/[.03] p-3 hover:border-cyan-300/50"><div class="aspect-[4/3] overflow-hidden rounded-2xl bg-gradient-to-br from-cyan-300/30 via-slate-800 to-violet-500/30">@if($project->cover_image)<img src="{{ asset('storage/'.$project->cover_image) }}" class="h-full w-full object-cover" alt="{{ $project->title }} cover">@endif</div><div class="p-4"><h2 class="text-xl font-semibold text-white">{{ $project->title }}</h2><p class="mt-2 text-sm leading-6 text-slate-400">{{ $project->short_description }}</p><div class="mt-4 flex flex-wrap gap-2">@foreach($project->technologies as $tech)<span class="rounded-full bg-white/5 px-2.5 py-1 text-xs">{{ $tech->name }}</span>@endforeach</div></div></a>@empty<div class="col-span-full rounded-3xl border border-dashed border-white/15 p-12 text-center text-slate-400">No projects match your filters. Try a different search or clear the filters.</div>@endforelse</div><div class="mt-10">{{ $projects->links() }}</div></section>
@endsection
