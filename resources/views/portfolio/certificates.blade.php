@extends('layouts.portfolio')
@section('content')
<section class="mx-auto max-w-7xl px-6 py-24"><p class="font-mono text-sm text-orange-500">04 — Credentials</p><h1 class="mt-3 text-5xl font-bold text-white">Certificates</h1><div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse($certificates as $certificate)<article class="overflow-hidden rounded-3xl border border-white/10 bg-white/[.03]"><div class="flex aspect-[16/10] items-center justify-center bg-[#181818] p-4">@if($certificate->image_path)<img src="{{ asset('storage/'.$certificate->image_path) }}" class="h-full w-full object-contain" alt="{{ $certificate->title }} certificate">@else<span class="text-5xl text-orange-500">✦</span>@endif</div><div class="p-6"><p class="text-sm text-orange-500">{{ $certificate->issue_date?->format('M Y') }}</p><h2 class="mt-2 text-xl font-semibold text-white">{{ $certificate->title }}</h2><p class="mt-1 text-white/55">{{ $certificate->issuer }}</p>@if($certificate->credential_id)<p class="mt-4 text-xs text-white/40">Credential ID: {{ $certificate->credential_id }}</p>@endif<div class="mt-5 flex gap-3">@if($certificate->url)<a href="{{ $certificate->url }}" target="_blank" rel="noreferrer" class="rounded-full bg-orange-500 px-4 py-2 text-sm font-semibold text-white">View credential ↗</a>@elseif($certificate->image_path)<a href="{{ asset('storage/'.$certificate->image_path) }}" target="_blank" class="rounded-full border border-white/20 px-4 py-2 text-sm text-white/70">View certificate</a>@endif</div></div></article>@empty<div class="col-span-full rounded-3xl border border-dashed border-white/15 p-12 text-center text-white/45">Certificates will appear here once added.</div>@endforelse</div></section>
@endsection
