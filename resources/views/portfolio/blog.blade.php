@extends('layouts.portfolio') @section('content')<section class="mx-auto max-w-7xl px-6 py-24"><p class="font-mono text-sm text-cyan-300">05 — Notes</p><h1 class="mt-3 text-5xl font-bold text-white">Writing</h1><form class="mt-8 flex gap-2"><input name="q" value="{{ request('q') }}" placeholder="Search articles" class="rounded-full border border-white/15 bg-white/5 px-4 py-3 text-sm outline-none"><button class="rounded-full bg-cyan-300 px-5 text-sm font-semibold text-slate-950">Search</button></form><div class="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse($blogs as $post)<a href="{{ route('blog.show',$post) }}" class="rounded-3xl border border-white/10 bg-white/[.03] p-7 hover:border-cyan-300/50"><p class="text-sm text-cyan-300">{{ $post->published_at?->format('M d, Y') }}</p><h2 class="mt-4 text-2xl font-semibold text-white">{{ $post->title }}</h2><p class="mt-3 leading-7 text-slate-400">{{ $post->excerpt }}</p></a>@empty<p class="text-slate-400">No published articles yet.</p>@endforelse</div><div class="mt-10">{{ $blogs->links() }}</div></section>@endsection
