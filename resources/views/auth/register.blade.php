@extends('layouts.portfolio')
@section('content')
<section class="mx-auto max-w-xl px-6 py-24"><h1 class="text-4xl font-bold text-white">Create your portfolio</h1><p class="mt-3 text-white/60">Choose a username to get your public portfolio link.</p><form method="POST" action="{{ route('register.store') }}" class="mt-8 space-y-5">@csrf @foreach([['name','Name','text'],['username','Username','text'],['email','Email','email'],['password','Password','password'],['password_confirmation','Confirm password','password']] as [$name,$label,$type])<label class="block text-sm text-white/70">{{ $label }}<input required name="{{ $name }}" type="{{ $type }}" value="{{ old($name) }}" class="mt-2 w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white">@error($name)<span class="text-xs text-red-400">{{ $message }}</span>@enderror</label>@endforeach<button class="rounded-xl bg-orange-500 px-6 py-3 font-semibold text-white">Create account</button></form><p class="mt-6 text-sm text-white/60">Already registered? <a class="text-orange-400" href="{{ route('login') }}">Sign in</a></p></section>
@endsection
