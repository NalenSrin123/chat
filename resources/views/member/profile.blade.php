@extends('layouts.portfolio')
@section('content')
<section class="mx-auto max-w-5xl px-6 py-20"><a class="text-orange-400" href="{{ route('member.dashboard') }}">← Dashboard</a><h1 class="mt-4 text-4xl font-bold text-white">Your portfolio profile</h1><form method="POST" action="{{ route('member.profile.update') }}" class="mt-8 grid gap-5 md:grid-cols-2">@csrf @method('PUT') @foreach([['name','Name'],['username','Username'],['title','Professional title'],['location','Location'],['website','Website']] as $field)<label class="text-sm text-white/70">{{ $field[1] }}<input name="{{ $field[0] }}" value="{{ old($field[0],$field[0]==='name'?$user->name:$user->profile->{$field[0]}) }}" class="mt-2 w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white"></label>@endforeach<label class="text-sm text-white/70 md:col-span-2">Bio<textarea name="bio" rows="6" class="mt-2 w-full rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white">{{ old('bio',$user->profile->bio) }}</textarea></label><button class="w-fit rounded-xl bg-orange-500 px-6 py-3 font-semibold text-white">Save profile</button></form></section>
@endsection
