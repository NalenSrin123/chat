@extends('layouts.portfolio')
@section('content')
<section class="mx-auto max-w-6xl px-6 py-24"><p class="font-mono text-orange-400">PORTFOLIO / {{ '@'.$user->username }}</p><h1 class="mt-5 text-6xl font-bold text-white">{{ $user->name }}</h1><p class="mt-4 text-2xl text-orange-400">{{ $user->profile->title }}</p><p class="mt-6 max-w-3xl text-lg leading-8 text-white/65">{{ $user->profile->bio }}</p><h2 class="mt-24 text-3xl font-bold text-white">Selected work</h2><div class="mt-8 grid gap-6 md:grid-cols-2">@forelse($user->projects->where('status','!=','draft') as $project)<article class="rounded-2xl border border-white/10 bg-white/5 p-6 text-white"><h3 class="text-xl font-semibold">{{ $project->title }}</h3><p class="mt-2 text-white/60">{{ $project->short_description }}</p><a class="mt-5 inline-block text-orange-400" href="{{ route('projects.show',$project->slug) }}">View project →</a></article>@empty<p class="text-white/60">This portfolio has no public projects yet.</p>@endforelse</div></section>
@endsection
