@extends('layouts.portfolio')
@section('content')
<section class="mx-auto max-w-6xl px-6 py-20"><a class="text-orange-400" href="{{ route('member.dashboard') }}">← Dashboard</a><div class="mt-4 flex items-center justify-between"><h1 class="text-4xl font-bold text-white">Your projects</h1><a class="rounded-xl bg-orange-500 px-5 py-3 font-semibold text-white" href="{{ route('member.projects.create') }}">Add project</a></div><div class="mt-8 grid gap-5 md:grid-cols-2">@forelse($projects as $project)<article class="rounded-2xl border border-white/10 bg-white/5 p-6 text-white"><h2 class="text-xl font-semibold">{{ $project->title }}</h2><p class="mt-2 text-white/60">{{ $project->short_description }}</p><form method="POST" action="{{ route('member.projects.destroy',$project) }}" class="mt-5">@csrf @method('DELETE')<button class="text-sm text-red-400">Delete</button></form></article>@empty<p class="text-white/60">No projects yet.</p>@endforelse</div><div class="mt-8">{{ $projects->links() }}</div></section>
@endsection
